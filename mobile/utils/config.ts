const config = {
  API_HOST: `${process.env.EXPO_PUBLIC_API_HOST}`,
  AUTH_CLIENT_ID: `${process.env.EXPO_PUBLIC_AUTH_CLIENT_ID}`,
  AUTH_DOMAIN: `${process.env.EXPO_PUBLIC_AUTH_DOMAIN}`,
  AUTH_USER_POOL_ENDPOINT: `${process.env.EXPO_PUBLIC_AUTH_USER_POOL_ENDPOINT}`,
};

export default config;
